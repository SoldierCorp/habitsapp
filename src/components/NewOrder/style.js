import { StyleSheet } from 'react-native';
import { whitesmoke } from '../../assets/styles/colors'

const styles = StyleSheet.create({
	screen: {
		backgroundColor: whitesmoke,
	},
	input: {
		marginBottom: 10
	},
	picker: {
		justifyContent: 'center',
		paddingHorizontal: 0,
		paddingLeft: 5
	},
})

export default styles
